version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  hello-python:
    lang: python3-http-debian
    handler: ./hello-python
    image: localhost:5000/nlp:latest
    build_args:
      ADDITIONAL_PACKAGE: 'g++ cmake unzip libcurl4-openssl-dev'
      TEST_ENABLED: false
